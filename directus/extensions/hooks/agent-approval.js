// M2 — implemented when the agent self-registration flow is built.
// Fires when agents.approved transitions false → true; sends approval email via worker.
export default ({ action }, { services, env }) => {};
