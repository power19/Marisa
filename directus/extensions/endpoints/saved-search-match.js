// M2 — implemented when saved searches and the background worker are built.
// Internal endpoint called by the worker to fetch saved_searches + match against listings.
export default (router, { services, env }) => {};
